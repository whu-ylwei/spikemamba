# 残差桥 Flow 诊断与增强理论文档（2026-08-04）

本文档三部分：(A) 停训与实证诊断结论；(B) 为什么"之前的 flow 训练反而劣化性能"的机理分析；
(C) 用 flow 做**小目标/远距离**定向增强的理论方案（含从 spike 读取更多条件的架构设想）。

**本文档只做理论分析，不改代码。** 后续若采纳再落地。

承接：[[reB-latentfix-training-0803]]、[[reB-scaleB-fix-training-0803]]、
[[reB-eval-pipeline-and-precision-0730]]、[[backbone-actual-resolution-224-not-112]]、
`LATENT_MANIFOLD_FIX_20260803.md`、`manifold反思.md`。

---

## A. 实证诊断结论（2026-08-04，latentfix batched 训到 ep16 停止）

### A.1 统一口径后的三方对比（DENSE test，evaluation_DENSE，total metrics 档）

| 对象 | 口径 | abs_rel(total) | abs_rel(_500) | δ<1.25(_500) |
|---|---|---|---|---|
| 0320 主干（参照天花板） | 224² CenterCrop | **0.597** | 0.707 | 0.636 |
| latentfix **coarse**（ep12，=冻结主干输出） | 224² CC | **0.595** | 0.706 | 0.634 |
| latentfix **refined**（ep12，coarse+flowΔ） | 224² CC | **0.597** | 0.710 | 0.636 |

**结论**：`refined ≈ coarse ≈ 0320`，三者在任一档位内逐项相等（差异在千分位=数值噪声）。
**flow 头零增益**：refined 的残差 Δ≈0，最终深度基本复刻 coarse。这解释了训练时
val_loss 从 ep1 就平在 0.0111（train_loss 持续降是 aux 项在降，与最终深度质量无关）。

### A.2 一个曾长期误导判断的口径陷阱（必须记住）

- 历史 memory 把 0320 记作 abs_rel **0.597**（取自 evaluation_DENSE 的 **total metrics 档**）。
- 把残差桥历史"最优"记作 **1.900**（取自 **_500 档**）。
- **这是两个不同档位混着比**。evaluation_DENSE 输出多档：`_10/_20/_80/_250/_500` 是按
  clip 距离分段的档，`total metrics` 是全量聚合档，同一模型不同档数值差很大
  （0320 自己：total=0.597 vs _500=0.707）。
- **今后所有对比必须锁定同一档**。本文档统一用 total 档。

### A.3 "残差桥比 0320 差 3.2×"是历史口径错误，非模型缺陷

- 旧结论"残差桥 abs_rel 1.900 是 0320 的 3.2×"来自：残差桥用 **resize→112²**、0320 用
  **CenterCrop 224²**，分辨率+视场+裁剪方式三重不同口径硬比（见 [[backbone-actual-resolution-224-not-112]]）。
- 喂对 224² CenterCrop 后，coarse（纯主干）total abs_rel 直接就是 **0.595**。
  所谓"1.900"是喂错分辨率的老账，不是残差桥模型本身差 3.2×。

---

## B. 为什么"之前的 flow 训练反而劣化性能"

### B.1 同口径（224² CC，total 档）的历史 flow 训练成绩

| 轮次 | 主干状态 | AE状态 | flow输出 abs_rel(total) | 相对主干0.595 |
|---|---|---|---|---|
| 0731 centercrop224 flow(best ep3) | **随机初始化，联合训** | 未冻 | **1.543** | **劣化 2.6×** |
| 0801 centercrop224 40+40(ep4) | 随机初始化，联合训 | 未冻 | 1.706 | 劣化 2.9× |
| **0804 latentfix refined(ep12)** | **0320预训练+冻结** | **预训练+冻结** | **0.597** | **持平（不劣化）** |

关键分水岭：**主干是否冻结/预训练**。

### B.2 机理：联合训练时 flow 头污染了主干（0731/0801 劣化的根因）

0731/0801 那两轮是**从随机初始化联合训练整个残差桥**（主干 + flow 头一起用一个 loss 训）：

1. **coarse 不再是 0320 的 coarse**。主干参数被 flow 的联合 loss 一起更新，coarse 输出偏离了
   0320 那个 0.595 的良态解。换句话说，联合训练把"本来很好的主干"往下拽了。
2. **梯度耦合污染**。`refined = coarse + Δ`，loss 对 coarse 和 Δ 同时回传。当 Δ 还没学好时，
   为降低 refined 的 loss，优化器会同时调 coarse——但 coarse 由庞大主干产生、且 0320 已是近最优，
   任何"迎合半成品 Δ"的调整都是净损失。
3. **从随机初始化 20~40ep 远不够**。0320 主干本身训了 200ep（100+100）。联合从头训 20-40ep，
   主干根本没收敛到 0.595，coarse 停在劣质解，refined 自然更差。
4. **loss 权重稀释**。`coarse_weight=1.0` 是最大项，但它监督的 coarse 在联合训练里是"可动且没训好"的，
   等于用一个大权重把主干拖向半成品状态。

**一句话：0731/0801 的劣化不是 flow 本身的错，是"让 flow 的 loss 反向污染了一个本已近最优的主干"。**

### B.3 为什么 latentfix（本轮）不劣化，但也不增益

latentfix 用 DepthFM 式两阶段**冻结主干+冻结AE**：

- **不劣化的原因**：主干冻结 → coarse 恒等于 0320 的 0.595，flow 的 loss 碰不到主干，无法污染它。
  这是本轮相比 0731/0801 的**纯改进**：至少守住了主干的成绩。
- **不增益的原因**：flow 头要学的是"在 0320 已达 0.595 的基础上再修正"。但——
  - 0320 已是该数据+该分辨率下主干能达到的近最优，**残差空间里没有系统性的、可从
    coarse latent 预测出来的结构性误差**供 flow 学习；
  - flow 的条件只有 `condition_latent`（coarse 深度编码）+ `encoder_feature_scale=0`（未接入
    主干特征），信息量 ⊆ coarse 自身，**无法提供 coarse 里没有的新信息**去修正 coarse 的错误；
  - 于是最优解就是 Δ→0（恒等映射），refined=coarse。val 从 ep1 平即此。

### B.4 综合结论

| 范式 | 结果 | 本质 |
|---|---|---|
| 联合训练（0731/0801） | 劣化 | flow loss 污染近最优主干 |
| 冻结主干+flow精修（latentfix） | 持平、零增益 | 残差无结构可学 + 条件信息⊆coarse |

**残差 flow 精修要产生增益，必须满足：flow 能获得 coarse 里没有的额外信息（新条件），
且存在 coarse 系统性做不好、但该额外信息能补上的区域。** 这正是 Part C 的出发点。

---

## C. 理论方案：用 flow 做小目标/远距离定向增强

### C.1 靶点确认——主干的系统性弱区就在远处（实证）

0320 主干各距离档（DENSE test）：

| 档(米) | abs_rel | δ<1.25 |
|---|---|---|
| _10 | 0.274 | **0.989** |
| _20 | 0.374 | 0.946 |
| _30 | 0.559 | 0.896 |
| _80 | 0.696 | 0.714 |
| _250 | 0.707 | 0.636 |
| _500 | 0.707 | **0.636** |

**近处近乎完美（δ 0.989），远处显著退化（δ 0.636）。** 误差高度集中在远距离/小目标。
这就是"coarse 系统性做不好的区域"——满足 Part B.4 增益的**第一个**必要条件。

**为什么主干在远处崩**（机理，决定能否修）：
- **量程解码放大**：深度用 `exp(reg_factor·x)`（reg_factor=5.7）解码，远景 x→1 时指数放大，
  latent 里微小误差在米空间被指数放大成大误差。
- **spike 远景信息稀疏**：远处物体在 spike 帧上占像素少、事件率低、信噪比差，主干
  下采样后远处小目标的特征被稀释/丢失。
- **训练信号偏近景**：SI/L1 类损失被近景大量像素主导，远景欠拟合。

### C.2 关键缺口——当前 flow 拿不到"新信息"（Part B.4 第二个条件未满足）

当前 flow 的条件仅 `condition_latent = E(coarse_depth)`，且 `encoder_feature_scale=0`
（主干中间特征未接入）。信息量 ⊆ coarse。**flow 无法修正 coarse 里已经丢失的远景信息**——
你不能从"已经糊掉的粗深度"里恢复出粗深度里没有的细节。这是 latentfix 零增益的根因。

**要让 flow 在远处产生增益，必须给它 coarse 里没有的、且富含远景信息的新条件。**
最自然的来源：**原始 spike 数据本身**（主干下采样前的高分辨率时空信息）。

### C.3 架构设想：spike-conditioned flow（从 spike 读取更多条件）

核心思想：flow 头不再只看 coarse latent，而是**额外接入从原始 spike 提取的、保留远景/高频
细节的条件特征**，专门去修 coarse 的远景误差。

```
                    ┌─ 冻结 0320 主干 ──→ coarse_depth ─┐
   raw spike ──────┤                                    ├─→ condition_latent = E(coarse)
   (128,224,224)   └─ [新] spike条件编码器 C_ψ ──────────┘         │
                        (轻量、可训、高分辨率保留)                    │
                                    │                               ▼
                                    └──→ spike_cond_feat ──→ velocity_field(z_t, [cond_latent ⊕ spike_cond_feat], t)
                                                                     │
                                                          refined = coarse + Δ(spikeΔ精修)
```

**要点**：
1. **新增可训模块 `spike条件编码器 C_ψ`**：直接吃 raw spike（或其高频/事件率统计），输出
   与 latent 同分辨率的条件图。它保留主干下采样丢掉的远景高频信息——这是 coarse 里没有的"新信息"。
2. **velocity_field 的条件从 `[cond_latent]` 扩为 `[cond_latent ⊕ spike_cond_feat]`**
   （通道拼接）。这是最小改动点（现 `_ConditionalVelocityField` 已接受 condition_latent，
   扩通道即可）。
3. **主干仍冻结**（守住 coarse 的 0.595，不重蹈 B.2 联合训练污染的覆辙）。只训 flow + C_ψ。

**可选的 spike 条件设计（按信息增量排序）**：
- (a) **事件率/时间统计图**：每像素的 spike 计数、首次/末次事件时间、时间质心——远景稀疏事件
  的显式统计，主干可能平均掉了。
- (b) **高分辨率 spike 特征**：用小 CNN 在**原始 224²（不下采样）**上提特征，保留远景小目标空间细节。
- (c) **多时间窗聚合**：不同积分窗的 spike（短窗抓快速远景运动，长窗抓静态结构）。

### C.4 让增强"定向"到远处——损失与门控设计

即使给了新条件，还需引导 flow **只在远处发力、别扰动已近最优的近景**：

1. **远景加权损失**（与现有 `far_downweight_gamma` 反向）：新增 `far_UPweight`，对远景像素
   （GT 深度大 / x→1）**放大** refined 的监督权重，让 flow 的梯度集中在远处。
2. **近景残差正则**：对近景区域加 `‖Δ‖²` 惩罚，强制近处 Δ→0（保持 coarse），只放开远处。
   即"空间选择性残差"。
3. **不确定性门控（进阶）**：让主干/flow 输出一个逐像素置信度，Δ 只在低置信区（远景/小目标）生效：
   `refined = coarse + gate·Δ`，gate 由置信度驱动。等价于"只在主干没把握的地方让 flow 介入"。

### C.5 为什么这套在理论上可能有增益（对照 Part B.4 的两个必要条件）

| 必要条件 | 当前 latentfix | 本方案 |
|---|---|---|
| ① 存在 coarse 系统性做不好的区域 | ✅ 远景（δ 0.989→0.636） | ✅ 同 |
| ② flow 能拿到 coarse 里没有的新信息 | ❌ 条件⊆coarse | ✅ spike 高分辨率/事件统计条件 |

latentfix 只满足①不满足②→零增益。本方案补上②→**理论上**能修远景。

### C.6 风险与开放问题（落地前必须先想清楚）

1. **spike 条件是否真含 coarse 没有的远景信息**？需先做**信息量诊断**：训一个"只从 spike 直接
   回归远景深度"的探针，看远景 abs_rel 能否低于主干——若不能，说明 spike 本身远景信息就不足，
   再精修也无用（数据上限问题，非架构问题）。**这是落地前的第一道 gate。**
2. **远景 GT 质量**：DENSE 远景 GT 本身可能噪声大（LiDAR 稀疏/量程边界），监督信号不可靠会
   让"增强"变"拟合噪声"。需先查远景 GT 有效像素比例与可信度。
3. **exp 解码放大**：远景在米空间误差被指数放大，训练要不要改到 log 空间直接监督远景、或
   对远景单独用线性空间 L1（现有 `refined_metric` 是线性空间，可复用并加远景权重）。
4. **联合训 C_ψ 的显存**：C_ψ 在 224² 原分辨率上跑，激活比在 latent 上大得多，
   会显著增加显存（参考本轮 batch 化的 host RAM / GPU 权衡，见 [[reB-stage2-batching-speedup-0804]]）。
5. **是否值得**：主干远景差可能是**数据/传感器物理上限**（远处 spike 信息本就稀疏），
   若如此，任何后处理都触不到天花板，应转向**改主干训练**（远景加权、更高输入分辨率）
   而非加 flow 后处理。C.6.1 的探针能回答这个。

### C.7 与"直接改主干"路线的取舍（不引入 flow 的替代方案）

既然 latentfix 证明"冻结主干+flow 精修"零增益，另一条路是**直接改进主干本身**，可能更简单有效：
- **远景加权重训主干**：0320 用 SI 损失被近景主导，改成远景加权 loss 重训/微调主干。
- **更高输入分辨率**：主干喂 >224² 或不下采样那么狠，直接保留远景细节。
- **多尺度主干**：远景用更浅下采样的分支。

**决策建议**：先做 C.6.1 的 spike 远景信息探针 →
- 若 spike 远景信息充足但主干没用好 → 优先"改主干"（更简单，不引入 flow 复杂度）；
- 若主干架构受限、但 flow+spike 条件能榨出额外信息 → 上 C.3 的 spike-conditioned flow；
- 若 spike 远景信息本就不足 → 两条路都无解，是数据/传感器上限，需换数据或接受天花板。

---

## D. 下一步（仅规划，未执行）

1. **[gate] spike 远景信息探针**：验证 spike 是否含 coarse 缺失的远景信息（C.6.1）。这是所有
   flow 增强方案的前置判据，不过这关后面都免谈。
2. 若过 gate：实现 C.3 的 `spike条件编码器 C_ψ` + velocity_field 扩条件通道 + C.4 定向损失。
3. 对照实验：始终冻结主干（守住 0.595），只看 refined 在**远景档（_250/_500）**是否超过 coarse。
   判据锁 total + _500 双档，避免 A.2 的档位陷阱。
4. 保留本轮 latentfix 的 stage-1 AE ep29 快照可复用（[[reB-stage2-batching-speedup-0804]]）。

**本文档为理论分析，未改任何代码/训练。实施前需先过 D.1 探针 gate。**

