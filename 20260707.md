# SpikeMamba 架构分析

从脉冲相机 (spike camera) 数据做单目深度估计的项目。整体是 **3D Mamba 编码器 + U-Net 解码器 (+ 可选的 Manifold Flow 精化头)**,在时序序列上循环推理。

## 一、模块划分与职责

```
数据集 → 编码器 → 解码器 → (Manifold Flow) → 深度图
spike_dataset  mamba_3d   S2DepthNet   manifold_flow
```

| 层级 | 文件 | 类 | 职责 |
|---|---|---|---|
| 数据 | `data_loader/spike_dataset.py` | `VoxelGridDENSESpikeDataset` | 从 `.npy` 读单帧 voxel-grid 脉冲张量,按非零值做均值/方差归一化 |
| 数据 | `data_loader/SpikesDENSE_dataset.py` | `SequenceSynchronizedFramesSpikesDENSEDataset` | 把 spike 张量与深度 GT 时间对齐,组织成长度 L 的序列,支持随机"暂停"增强 |
| 顶层模型 | `model/model.py` | `BaseERGB2Depth` | 只做配置解析(bins/编码器数/通道数/skip 类型等) |
| 主网络 | `model/S2DepthNet.py` | `S2DepthTransformerUNetConv` | 组装 encoder + resblocks + decoder + 预测头 + flow,负责输入形状规整和 forward |
| 编码器包装 | `model/encoder_transformer.py` | `LongSpikeStreamEncoderConv` | 调用 3D backbone,再把 3D 特征沿时间分块投影成 2D 特征供解码器用 |
| 编码器主干 | `model/mamba_3d.py` | `Mamba3DEncoder` / `ST_Mamba_Block` | 真正的双向 Mamba SSM 时空编码 |
| 精化头 | `model/manifold_flow.py` | `ConditionedDepthManifoldFlow` | 可选的 flow-matching 深度精化 |
| 构件 | `model/submodules.py` | `ConvLayer/ResidualBlock/UpsampleConvLayer` | 卷积/残差/上采样基础块 |
| 损失 | `model/loss.py` | scale-invariant / MSE / 多尺度梯度 | 主损失 + 辅助损失 |
| 训练 | `trainer/spiket_trainer.py` | `SpikeTTrainer` | 时序循环前向、loss 聚合、可视化 |

## 二、数据流(推理路径)

**输入**:一帧脉冲张量 `[B, C, H, W]`,C = `num_bins_rgb`(voxel-grid 的时间 bin 数,配置里叫 r128 → 128 bins)。

**1. 形状规整** (`S2DepthNet.forward`, line 175-221)
- 把 4D `[B,C,H,W]` 的 bin 维当作时间维,`unsqueeze(1)` → `[B, 1, C, H, W]` = `[B, in_chans=1, T, H, W]`。5D 输入也统一到这个布局。
- 这里 spike 的时间 bin 被显式当成 3D 卷积的时间轴。

**2. Patch embedding** (`Mamba3DEncoder`, line 108)
- `Conv3d(1, embed_dim, kernel=patch_size, stride=patch_size)`,`patch_size=(T_p, 2, 2)`。时间和空间同时被 patch 化。

**3. 多阶段 Mamba 编码** (line 130-144)
每个 stage:`MambaStage`(若干 `ST_Mamba_Block`) → 空间下采样 `Conv3d(kernel=(1,2,2))`(只降空间,**保留时间分辨率**)。通道逐阶翻倍 `embed_dim × 2^i`。在 `out_indices` 处收集多尺度特征。

`ST_Mamba_Block` 内部(line 64-89)是核心:
- `[B,C,T,H,W]` → permute 到 `[B,H,W,T,C]`
- **2×2 空间折叠**:把每个 2×2 邻域折进通道,得到序列 `[B, T·H/2·W/2, 4C]`(`_fold_spatial_tokens`)
- **双向 Mamba**:`mamba(x)` 前向 + `mamba_bwd(flip(x))` 反向,二者取平均。反向分支共享前向的 `in_proj/out_proj`,只有 SSM 参数不同(仿 Vim BiMamba v2)
- unfold 回空间,残差加回;再接 LayerNorm + MLP(FFN)残差

> 注意:`CLAUDE.md` 记录了一个设计意图——把这两处 `x = x + x1` / `x = x + x2` 残差换成 `MHCResidual`(多头交叉注意力)。当前 `mamba_3d.py` 里仍是简单加法,说明这是待实现/进行中的方向。要动这块前值得先确认最新分支状态。

**4. 3D→2D 投影** (`LongSpikeStreamEncoderConv.forward`, line 58-83)
每个尺度的 3D 特征沿时间维 chunk 成 `num_blocks = T//patch_size[0]` 块,每块 reshape 后用 `1×1 Conv2d` 压到 `C//num_blocks`,再拼回。得到多尺度 2D 特征图列表,供 U-Net 解码。

**5. U-Net 解码** (`S2DepthNet.forward_decoder`, line 117-149)
- 取最深特征 → 过 `num_residual_blocks` 个残差块
- 逐级 `UpsampleConvLayer` 上采样,用 skip connection(默认 `sum`)融合对应编码器尺度特征
- `pred` (1×1 Conv) 输出单通道
- 可配置输出头:identity/sigmoid/softplus + scale/shift(避免 sigmoid 饱和)

**6. (可选) Manifold Flow 精化** (`manifold_flow.py`)
若 `manifold_flow.enabled`:
- coarse 深度 → `condition_encoder` 下采样成 latent,并融合编码器特征作为条件
- **flow matching**:从噪声/零出发,用 `_ConditionalVelocityField` 做 `flow_steps` 步欧拉积分 rollout latent → decoder 出精化深度
- 训练时还编码 GT 深度,产生重建损失 `L_rec`、flow 损失 `L_flow`、时序几何一致性损失 `L_geo`,以及 coarse 监督 `L_coarse`

## 三、训练数据流 (`SpikeTTrainer.forward_pass_sequence`, line 265)

时序循环,逐帧带状态推理:
```
for l in range(L):                       # L = sequence_length (如 2/4)
    pred, super_states, states_lstm =
        model(item, prev_super_states['image'], prev_states_lstm)
    # 主损失: scale_invariant_loss(pred, depth_gt)
    # + 多尺度梯度损失 (grad_loss)
    # + flow 辅助损失 (L_coarse/L_rec/L_flow/L_geo)
    prev_super_states, prev_states_lstm = 更新       # 跨帧传递状态
```
- 主损失:**尺度不变损失**(单目深度标准做法)+ 权重 0.25 的多尺度梯度损失
- 辅助 flow 损失在 `_add_extra_losses_to_total` 里按序列长度平均后加进总 loss
- 指标:`mse / abs_rel_diff / scale_invariant_error / median_error`
- 数据集:DENSE(CARLA 合成),`spike/r128` voxel grid,深度 `depth/data`,`clip_distance=1000`

## 关键设计点

1. **spike 的时间 bin = 3D 时序轴**,用 3D patch + 双向 Mamba 建模时空,而非把 bins 当普通通道。
2. **空间 2×2 折叠成序列**让 Mamba 的 1D 扫描能覆盖空间,同时缩短序列长度(4C 通道换 1/4 长度)。
3. **Mamba 是 Swin3D 的 drop-in 替换**(`swin_transformer_3d.py` 是历史 backbone,注释里明说 Mamba3DEncoder 替换它),整体是从 spike-transformer 演进到 spike-mamba 的项目。
4. **Manifold Flow 是可插拔精化头**,不开时就是标准 encoder-decoder。

---

## 冒烟测试记录 (系统 Python, 2026-07-07)

**目标**:用 shared 存储上的系统 Python (`/usr/bin/python3`, 3.12.3) 而非 `.venv` 跑一次冒烟测试。

### 环境准备
- 系统 Python 已有 `torch 2.7.0a0+...nv25.04` (CUDA 可用)、torchvision、numpy、tensorboard、matplotlib、imageio。
- 缺失依赖,已安装到 `/usr/local/lib/python3.12/dist-packages`:
  - `mamba_ssm 2.2.2`(仓库内自带 wheel,`--no-deps` 安装,避免拉低 torch)
  - `kornia 0.8.3`、`kornia_rs`、`opencv (cv2) 4.10.0`(`--no-deps`,常规 `pip install` 因依赖回溯 + 共享机并发 pip 卡死,故改 `--no-deps`)
- 数据路径:config 指向 `DENSE/train`,实际数据在 `DENSE-spike/DENSE/`,已建软链 `DENSE -> DENSE-spike/DENSE`。

### 验证通过的部分
- 所有关键 import 在系统 Python 下 OK(含 ABI 敏感的 `mamba_ssm`,torch2.4 编译的 wheel 在 torch2.7 下仅有 deprecation 警告,能加载)。
- 模型可完整构建:`S2DepthTransformerUNetConv`,**122,552,178 可训练参数**,含 manifold flow 全部子模块。
- 数据集加载正常:train 5 序列 / val 1 序列;`ds[0]` 得到长度 L=2 的序列,键 `image`(spike)+ `depth_image`。
- 真实数据流跑通到 SSM 之前:dataset → 形状规整 → `Conv3d` patch_embed → 进入 `ST_Mamba_Block`。

### 阻塞问题(硬件/环境,非代码 bug)
1. `train_parallel.py` 单卡路径有既有 bug:`DistributedSampler` 无条件创建,但非分布式时未 `init_process_group`。走 `--multiprocessing_distributed` 则在 `mp.spawn` 内 UCX (`libucs.so`) 段错误(NGC 容器常见)。故改用独立脚本 `smoke_systempy_20260707.py` 直接驱动 dataset→model→loss→backward。
2. **根因**:GPU 是 **RTX 5090(compute capability sm_120 / 12.0)**。系统 torch 2.7 支持 sm_120,但自带的 `mamba_ssm` wheel(`cu122torch2.4`)里的 `selective_scan_cuda` 内核只为旧架构编译,对 sm_120 报:
   ```
   RuntimeError: CUDA error: no kernel image is available for execution on the device
   （在 mamba_ssm/ops/selective_scan_interface.py selective_scan_cuda.fwd）
   ```
   `torch.cuda.get_arch_list()` = `[sm_75, sm_80, sm_86, sm_90, sm_100, sm_120, compute_120]`,而 mamba wheel 不含 sm_120 image。

### 修复:为 sm_120 从源码重编 mamba_ssm(已完成)
1. 卸载不兼容的自带 wheel(`cu122torch2.4`)。
2. PyPI sdist 的 `csrc/` 是空的(不含 CUDA 源),改从 git tag `v2.2.2` clone 完整源码:`git clone --depth 1 --branch v2.2.2 https://github.com/state-spaces/mamba.git`。
3. `setup.py` 硬编码 gencode(最高 sm_90)且**忽略** `TORCH_CUDA_ARCH_LIST`。手动把 gencode 列表改为**仅 `arch=compute_120,code=sm_120`**(既加了 Blackwell 支持,又把编译时间从全架构大幅缩短到 ~2min)。
4. 环境:nvcc 12.9、torch cuda 12.9、gcc 13.3、CUDA_HOME=/usr/local/cuda。
   ```
   export TORCH_CUDA_ARCH_LIST=12.0 MAMBA_FORCE_BUILD=TRUE CUDA_HOME=/usr/local/cuda MAX_JOBS=48
   python3 setup.py build_ext --inplace   # 编译 selective_scan_cuda (sm_120 单架构)
   pip install --no-build-isolation --no-deps .
   ```
5. 编译中的 `cub LaneId deprecated` 只是 warning,不影响。

### 最终结果:冒烟测试通过 ✅
`CUDA_VISIBLE_DEVICES=0 python3 smoke_systempy_20260707.py`:
```
Trainable parameters: 122552178
resized to: (112, 112)
step 0: pred (1,1,112,112) loss=0.3415 aux=[('L_rec',0.0473),('L_flow',0.1028)]
step 1: pred (1,1,112,112) loss=0.3407 aux=[('L_rec',0.0472),('L_flow',0.1019),('L_geo',0.0002)]
backward OK. total_loss=0.3411 grad_norm=0.0895
SMOKE TEST PASSED
```
- selective_scan CUDA 内核在 sm_120 (RTX 5090) 正常执行,不再报 "no kernel image"。
- 完整 dataset → model → 双步时序前向 → manifold flow 辅助损失 → backward → optimizer step 全通。

辅助文件:`smoke_systempy_20260707.py`(独立冒烟脚本,绕开 launcher 的 DistributedSampler/UCX 问题),旧 run 目录已备份为 `runs/train/train_s2d_MambaFlow_systempy_smoke_1ep_20260424.bak_20260707`。

### 备注(如需用 train_parallel.py 正式训练)
系统 Python 现已完全可用,但 `train_parallel.py` 本身仍有两个待处理点(与本次依赖修复无关):
- 单卡非分布式路径:`DistributedSampler` 无条件创建但未 `init_process_group` → 需加 `if args.distributed:` 判断或初始化单进程组。
- `--multiprocessing_distributed` 在本 NGC 容器内 `mp.spawn` 触发 UCX 段错误 → 可尝试 `export UCX_TLS=tcp` 或禁用 UCX,或直接改单进程训练。

